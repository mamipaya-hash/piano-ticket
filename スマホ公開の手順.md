# スマホで使うための手順

## 1. Supabaseを作る

Supabaseで新しいプロジェクトを作り、`supabase-schema.sql` をSQL Editorで実行します。

その後、Authentication > Users で講師用のメールアドレスとパスワードを登録します。

## 2. アプリにSupabaseを接続する

Supabaseの Project Settings > API Keys から以下をコピーします。

- Project URL
- Publishable key

それを `config.js` に貼ります。

## 3. GitHubへアップロードする

GitHubで新しいリポジトリを作り、このフォルダのファイルをアップロードします。

GitHub Pagesを有効にすると、スマホで開けるURLが発行されます。

## 4. スマホのホーム画面に追加する

iPhoneならSafariでURLを開き、共有ボタンから「ホーム画面に追加」を選びます。

AndroidならChromeでURLを開き、メニューから「ホーム画面に追加」を選びます。
